Regions as workspaces:

```mermaid
graph TB
    Client
    DBA[Database A]
    DBB[Database B]

    Client-->DBA
    Client-->DBB
```

```mermaid
graph TB
    Database
    DepA
    DepB
    DepC
    DepD

    Database-->DepA
    Database-->DepB
    DepA-->DepC
    DepA-->DepD
```

```jolie
workspace Local {
    // Would be nice if this automatically adjusted the input port of A, but it 
    // has potentially several
    outputPort A embeds A 

    outputPort B {
        Location: "socket://localhost:41000"
        Protocol: sodep
    }
}

workspace A {
    // Automatically inherits default configuration from "database".
    //
    // Also means that we can write --deploy A and the interpter knows what to
    // deploy
    FOR = "database" 

    USERNAME = "foo"
    PASSWORD = "bar"
    final ALGORITHM = "simple"
}

workspace B {
    FOR = "database"

    USERNAME = "foo"
    PASSWORD = "bar"
    final ALGORITHM = "advanced"

    inputPort Database {
        Location: "socket://localhost:41000"
        Protocol: sodep
    }
}
```

```
jolie --deploy A
```
